import React, { useState } from "react";
import { StyleSheet, Text, View, ActivityIndicator } from "react-native";

import Navegador from "./Navegacao";

export default function App() {
  return <Navegador />;
}
